interface:
  display_name: "Markdown AI Claim Graph"
  short_description: "Build claim graphs from AI Markdown"
  icon_small: "./assets/icon-small.svg"
  icon_large: "./assets/icon.svg"
  brand_color: "#1F6FEB"
  default_prompt: "Use $mag to convert Markdown analyst reports into a typed claim graph with node and edge tables, Mermaid, JSON, and a decision summary."

policy:
  allow_implicit_invocation: true
