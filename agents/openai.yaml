interface:
  display_name: "Markdown AI Cross-Review"
  short_description: "Compare AI-written Markdown analyses"
  default_prompt: "Use $markdown-ai-cross-review to compare multiple AI analysis Markdown files, highlight agreements and conflicts, and recommend what to do next."

policy:
  allow_implicit_invocation: true
